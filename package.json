{
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel client server",
    "server": "cd ./src/admin-api && php artisan serve --host=0.0.0.0",
    "client": "cd ./src/admin-vue && quasar dev"
  },
  "dependencies": {
    "npm-run-all": "^4.0.2"
  }
}
